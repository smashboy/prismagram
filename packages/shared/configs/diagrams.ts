export enum NodeType {
  MODEL = 'model',
  ENUM = 'enum'
}

export enum RelationIOType {
  SOURCE = 'source',
  TARGET = 'target'
}

export enum RelationType {
  ONE_TO_ONE = '1-1',
  ONE_TO_MANY = '1-n',
  MANY_TO_MANY = 'n-m'
}

export enum DiagramLayout {
  HORIZONTAL,
  VERTICAL
}
