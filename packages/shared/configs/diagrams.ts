export enum NodeType {
  MODEL = 'model',
  ENUM = 'enum'
}

export enum ScalarType {
  STRING = 'String',
  BOOLEAN = 'Boolean',
  INT = 'Int',
  BIG_INT = 'BigInt',
  FLOAT = 'Float',
  DECIMAL = 'Decimal',
  DATE_TIME = 'DateTime',
  JSON = 'Json',
  BYTES = 'Bytes'
}

export enum RelationIOType {
  SOURCE = 'source',
  TARGET = 'target'
}
